.eight.columns.centered
  %h2 Create a New Event

  - if @event.errors.any?
    #error-explanation
      %h3.error-title The following errors have occured:
      %ul
        - @event.errors.full_messages.each do |msg|
          %li= msg

  - if flash[:error]
    #error-explanation
      %h3= flash[:error]


  = form_for @event, :url => "/create_event" do |f|
    = f.label :title
    = f.text_field :title

    = f.label :description
    = f.text_area :description

    = f.submit 'Create Event'